/** Protocol version — bump whenever server↔client message shapes change. */
export const PROTOCOL_VERSION = '1.0.0' as const;

/** Minimum client protocol version the server will accept. */
export const MIN_CLIENT_VERSION = '1.0.0' as const;
